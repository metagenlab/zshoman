Changes in component 'nf-core/seqtk/subseq'
Changes in 'seqtk/subseq/main.nf':
--- modules/nf-core/seqtk/subseq/main.nf
+++ modules/nf-core/seqtk/subseq/main.nf
@@ -25,13 +25,14 @@
     if ("$sequences" ==~ /.+\.fq|.+\.fq.gz|.+\.fastq|.+\.fastq.gz/) {
         ext = "fq"
     }
+    def samplename = "$sequences.simpleName"
     """
     seqtk \\
         subseq \\
         $args \\
         $sequences \\
         $filter_list | \\
-        gzip --no-name > ${sequences}${prefix}.${ext}.gz
+        gzip --no-name > ${samplename}_${prefix}.${ext}.gz
     """
 
     stub:
@@ -40,7 +41,8 @@
     if ("$sequences" ==~ /.+\.fq|.+\.fq.gz|.+\.fastq|.+\.fastq.gz/) {
         ext = "fq"
     }
+    def samplename = "$sequences.simpleName"
     """
-    echo "" | gzip > ${sequences}${prefix}.${ext}.gz
+    echo "" | gzip > ${samplename}_${prefix}.${ext}.gz
     """
 }

'modules/nf-core/seqtk/subseq/environment.yml' is unchanged
'modules/nf-core/seqtk/subseq/meta.yml' is unchanged
'modules/nf-core/seqtk/subseq/tests/standard.config' is unchanged
'modules/nf-core/seqtk/subseq/tests/main.nf.test' is unchanged
'modules/nf-core/seqtk/subseq/tests/main.nf.test.snap' is unchanged
************************************************************
